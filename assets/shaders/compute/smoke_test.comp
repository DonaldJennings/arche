#version 450
layout(local_size_x = 64) in;

layout(set = 0, binding = 0) buffer DataBuffer { float data[]; };
layout(push_constant) uniform PC { uint count; };

void main() {
    uint i = gl_GlobalInvocationID.x;
    if (i < count) data[i] *= 2.0;
}
